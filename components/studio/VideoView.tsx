import React from 'react';

const VideoView = () => {
    return (
        <div className="p-4">
            <h3 className="text-xl font-semibold">Video Generation Playground</h3>
            <p>Generate videos with models like Veo.</p>
        </div>
    );
};

export default VideoView;
